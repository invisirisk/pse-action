{
  "name": "pse-action",
  "version": "1.0.0",
  "private": true,
  "description": "InvisiRisk PSE Security Proxy GitHub Action",
  "main": "src/index.js",
  "scripts": {
    "build": "node -e \"require('./src/index'); require('./src/post');\"",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}